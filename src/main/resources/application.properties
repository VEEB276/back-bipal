############# Database ###################
spring.datasource.url=jdbc:mysql://localhost:3306/bipal?useSSL=false&serverTimezone=America/Bogota
spring.datasource.username=root
spring.datasource.password=Vaesbu15951*
###only server###
#spring.datasource.jndi-name=${DATABASE_DGTH_DS}

server.servlet.context-path=/bipal
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jackson.time-zone=America/Bogota

app.client.url=*

#### Loggin settings #######
app.logs.directory=/ppp
app.logs.level=info
app.logs.console=true

########## Authorization #######################
authorization.seed=${JWT_SECRET:rsi}
authorization.enable.preflight=${ENABLE_PREFLIGHT:false}

########## Liquibase #######################
spring.liquibase.change-log: classpath:db/master.xml

########### TOPES MAXIMOS DE ARCHIVO #######
#Maximo del archivo para cada request
spring.servlet.multipart.max-file-size=10MB

#Maximo del multipart
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.resolve-lazily=true

microservice.initials=BIPAL